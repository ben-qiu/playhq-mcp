query Fixtures($gradeId: ID!) {
  discoverGrade(gradeID: $gradeId) {
    rounds {
      number
      id
      isFinalsRound
      isStale
      name
      abbreviatedName
      provisionalDate
    }
  }
}
