query DiscoverGrade($gradeId: ID!) {
  discoverGrade(gradeID: $gradeId) {
    id
    hideScores
    externalStatisticsUrls {
      url
    }
    dates
    season {
      id
      competition {
        id
        type
      }
    }
  }
}
