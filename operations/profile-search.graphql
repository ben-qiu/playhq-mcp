query ProfileSearch($fullName: String!) {
  profileSearch(fullName: $fullName) {
    result {
      firstName
      id
      lastName
      lastInteractedOrganisation {
        id
        name
      }
    }
  }
}
